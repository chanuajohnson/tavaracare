/**
 * Production base URL for all external-facing links.
 * Used in WhatsApp nudges, copy-link buttons, QR codes, invoices, etc.
 * Always points to the live domain — never the Lovable preview URL.
 */
export const PRODUCTION_BASE_URL = "https://tavara.care";
