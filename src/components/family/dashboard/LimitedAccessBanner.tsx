// Re-export from the shared, role-agnostic location so the family dashboard
// continues to import from the same path while the underlying component is
// reused for the professional dashboard as well.
export { LimitedAccessBanner } from '@/components/shared/LimitedAccessBanner';
export { default } from '@/components/shared/LimitedAccessBanner';
